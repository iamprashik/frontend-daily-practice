// Number to check
const number = 17;

// Check if the number is even or odd
if (number % 2 === 0) {
  console.log(`${number} is even.`);
} else {
  console.log(`${number} is odd.`);
}

// Another example
const secondNumber = 24;

if (secondNumber % 2 === 0) {
  console.log(`${secondNumber} is even.`);
} else {
  console.log(`${secondNumber} is odd.`);
}